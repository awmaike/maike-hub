"use client";
import { useEffect,useState } from "react";
type Section="inicio"|"servicos"|"projetos"|"maquinas"|"atalhos"|"configuracoes";type LinkItem={id:number;name:string;url:string;category:string};type SiteStatus={id:string;name:string;url:string;online:boolean;status:number;latencyMs:number;checkedAt:string};type Metrics={online:boolean;hostname:string;os:string;cpu:{percent:number;name:string;cores:number;threads:number};ram:{percent:number;usedGb:number;totalGb:number};disk:{percent:number;freeGb:number};gpu:{name:string;vramGb:number}[];network:{adapters:{name:string;speed:string}[];ipv4:string[]};processes:{name:string;pid:number;ramMb:number;cpuSeconds:number}[];services:{name:string;displayName:string}[];uptimeHours:number;agentVersion:string};
const LINKS_KEY="maike-hub:tech-links",AGENT_URL="http://127.0.0.1:43123/status";const initialLinks:LinkItem[]=[{id:1,name:"GitHub",url:"https://github.com",category:"Dev"},{id:2,name:"Vercel",url:"https://vercel.com",category:"Deploy"},{id:3,name:"Registro.br",url:"https://registro.br",category:"Domínios"},{id:4,name:"Tiflux",url:"https://tiflux.com",category:"Trabalho"}];
function NavButton({id,icon,label,section,onSelect}:{id:Section;icon:string;label:string;section:Section;onSelect:(id:Section)=>void}){return <button className={`navItem ${section===id?"active":""}`} onClick={()=>onSelect(id)}>{icon} <span>{label}</span></button>}
export default function Home(){const[section,setSection]=useState<Section>("inicio"),[links,setLinks]=useState(initialLinks),[name,setName]=useState(""),[url,setUrl]=useState(""),[hydrated,setHydrated]=useState(false),[metrics,setMetrics]=useState<Metrics|null>(null),[sites,setSites]=useState<SiteStatus[]>([]),[lastCheck,setLastCheck]=useState("");
useEffect(()=>{try{const s=localStorage.getItem(LINKS_KEY);if(s)setLinks(JSON.parse(s))}finally{setHydrated(true)}},[]);useEffect(()=>{if(hydrated)localStorage.setItem(LINKS_KEY,JSON.stringify(links))},[links,hydrated]);
useEffect(()=>{let a=true;async function poll(){try{const r=await fetch(AGENT_URL,{cache:"no-store"});if(!r.ok)throw 0;const d=await r.json();if(a)setMetrics(d)}catch{if(a)setMetrics(null)}}poll();const t=setInterval(poll,3000);return()=>{a=false;clearInterval(t)}},[]);
useEffect(()=>{let a=true;async function check(){try{const r=await fetch("/api/status",{cache:"no-store"});const d=await r.json();if(a){setSites(d.services||[]);setLastCheck(d.checkedAt||"")}}catch{}}check();const t=setInterval(check,60000);return()=>{a=false;clearInterval(t)}},[]);
function addLink(){const n=name.trim();let u=url.trim();if(!n||!u)return;if(!/^https?:\/\//i.test(u))u=`https://${u}`;setLinks(c=>[...c,{id:Date.now(),name:n,url:u,category:"Personalizado"}]);setName("");setUrl("")}
const badge=metrics?<span className="badge ok">AGENTE ONLINE</span>:<span className="badge warn">AGENTE OFF</span>;const site=(id:string)=>sites.find(s=>s.id===id);const hub=site("hub"),domain=site("domain");const statusBadge=(s?:SiteStatus)=>!s?<span className="badge neutral">VERIFICANDO</span>:s.online?<span className="badge ok">ONLINE</span>:<span className="badge warn">OFFLINE</span>;
const services=<div className="serviceGrid"><article className="serviceCard"><div className="serviceTop"><strong>Maike Hub</strong>{statusBadge(hub)}</div><p>hub.maikedev.com.br</p><small>{hub?`HTTP ${hub.status} · ${hub.latencyMs} ms`:"Verificando..."}</small></article><article className="serviceCard"><div className="serviceTop"><strong>Domínio principal</strong>{statusBadge(domain)}</div><p>maikedev.com.br</p><small>{domain?`${domain.status?`HTTP ${domain.status} · `:""}${domain.latencyMs} ms`:"Verificando..."}</small></article><article className="serviceCard"><div className="serviceTop"><strong>GitHub</strong><span className="badge ok">CONECTADO</span></div><p>awmaike/maike-hub</p><small>Deploy automático</small></article><article className="serviceCard"><div className="serviceTop"><strong>Windows</strong>{badge}</div><p>{metrics?.hostname||"Agente local"}</p><small>{metrics?`Agent v${metrics.agentVersion}`:"Aguardando agente"}</small></article></div>;
const machine=metrics?<><article className="panel"><div className="panelHeader"><div><p className="eyebrow">TELEMETRIA AO VIVO</p><h2>{metrics.hostname}</h2></div>{badge}</div><div className="metricCards"><MetricCard label="CPU" value={`${metrics.cpu.percent}%`} detail={`${metrics.cpu.cores||"—"} cores · ${metrics.cpu.threads||"—"} threads`} percent={metrics.cpu.percent}/><MetricCard label="RAM" value={`${metrics.ram.percent}%`} detail={`${metrics.ram.usedGb} / ${metrics.ram.totalGb} GB`} percent={metrics.ram.percent}/><MetricCard label="Disco C:" value={`${metrics.disk.percent}%`} detail={`${metrics.disk.freeGb} GB livres`} percent={metrics.disk.percent}/><MetricCard label="Uptime" value={`${metrics.uptimeHours}h`} detail={metrics.os}/></div>{metrics.processes&&<div className="processTable"><div className="processRow head"><span>Processo</span><span>PID</span><span>RAM</span><span>CPU</span></div>{metrics.processes.map(p=><div className="processRow" key={p.pid}><strong>{p.name}</strong><span>{p.pid}</span><span>{p.ramMb} MB</span><span>{p.cpuSeconds}s</span></div>)}</div>}</article></>:<article className="panel"><h2>Agente local</h2><p className="muted">Offline ou usando a versão anterior até a próxima reinicialização.</p></article>;
const shortcuts=<><div className="linkComposer"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome"/><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://..."/><button onClick={addLink}>Adicionar</button></div><div className="techLinks">{links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer"><span>{l.category}</span><strong>{l.name}</strong><small>{l.url.replace(/^https?:\/\//,"")}</small></a>)}</div></>;
let content;if(section==="servicos")content=<><Header eyebrow="MONITORAMENTO" title="Serviços" subtitle="Disponibilidade e latência verificadas pela Vercel."/><article className="panel"><div className="panelHeader"><div><p className="eyebrow">STATUS AO VIVO</p><h2>Endpoints</h2></div><span className="badge neutral">60 SEG</span></div>{services}{lastCheck&&<p className="checkTime">Última verificação: {new Date(lastCheck).toLocaleTimeString("pt-BR")}</p>}</article></>;else if(section==="maquinas")content=<><Header eyebrow="HARDWARE" title="Máquinas" subtitle="Telemetria local do Windows."/><div className="stack">{machine}</div></>;else if(section==="atalhos")content=<><Header eyebrow="FERRAMENTAS" title="Atalhos" subtitle="Sua central de acessos."/>{shortcuts}</>;else if(section==="projetos")content=<><Header eyebrow="DESENVOLVIMENTO" title="Projetos" subtitle="Código e deploys."/><article className="panel"><h2>Maike Hub</h2><p className="muted">Next.js · GitHub · Vercel</p></article></>;else if(section==="configuracoes")content=<><Header eyebrow="SISTEMA" title="Configurações" subtitle="Roadmap."/><article className="panel"><div className="roadmap"><p>01 — Telemetria Windows avançada ✅</p><p>02 — Monitoramento real de sites ✅</p><p>03 — GitHub: commits e repositórios</p><p>04 — Vercel: deploys e builds</p></div></article></>;else content=<><Header eyebrow="CENTRAL TÉCNICA" title="Maike Hub" subtitle="Infraestrutura, desenvolvimento e máquinas."/><section className="stats"><article className="statCard"><span>Hub</span><strong>{hub?.online?"Online":hub?"Offline":"..."}</strong><small>{hub?`${hub.latencyMs} ms`:"verificando"}</small></article><article className="statCard"><span>Máquina</span><strong>{metrics?"Online":"Offline"}</strong><small>{metrics?.hostname||"agente"}</small></article><article className="statCard"><span>CPU</span><strong>{metrics?`${metrics.cpu.percent}%`:"—"}</strong><small>{metrics?metrics.cpu.name:"sem agente"}</small></article><article className="statCard"><span>RAM</span><strong>{metrics?`${metrics.ram.percent}%`:"—"}</strong><small>{metrics?`${metrics.ram.usedGb}/${metrics.ram.totalGb} GB`:"sem agente"}</small></article></section><section className="dashboardGrid"><article className="panel wide"><div className="panelHeader"><div><p className="eyebrow">STATUS</p><h2>Infraestrutura</h2></div></div>{services}</article><article className="panel"><h2>Monitoramento</h2><p className="muted">Os sites são testados pelo servidor a cada minuto. O status não depende do seu computador estar ligado.</p><button className="primaryButton" onClick={()=>setSection("servicos")}>Ver detalhes</button></article></section></>;
return <main className="shell"><aside className="sidebar"><div className="brand"><div className="brandMark">M</div><div><strong>Maike Hub</strong><span>Tech Control</span></div></div><nav><NavButton id="inicio" icon="⌂" label="Visão geral" section={section} onSelect={setSection}/><NavButton id="servicos" icon="◉" label="Serviços" section={section} onSelect={setSection}/><NavButton id="projetos" icon="⌘" label="Projetos" section={section} onSelect={setSection}/><NavButton id="maquinas" icon="▣" label="Máquinas" section={section} onSelect={setSection}/><NavButton id="atalhos" icon="↗" label="Atalhos" section={section} onSelect={setSection}/><NavButton id="configuracoes" icon="⚙" label="Configurações" section={section} onSelect={setSection}/></nav><div className="sidebarBottom"><div className="statusDot"/><span>Hub online</span></div></aside><section className="content">{content}</section></main>}
function Header({eyebrow,title,subtitle}:{eyebrow:string;title:string;subtitle:string}){return <header className="topbar"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="muted">{subtitle}</p></div><div className="avatar">M</div></header>}function MetricCard({label,value,detail,percent}:{label:string;value:string;detail:string;percent?:number}){return <div className="metricCard"><span>{label}</span><strong>{value}</strong><small>{detail}</small>{typeof percent==="number"&&<div className="meter"><i style={{width:`${Math.min(100,Math.max(0,percent))}%`}}/></div>}</div>}
